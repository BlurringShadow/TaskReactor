<metro:MetroWindow x:Class="Presentation.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:local="clr-namespace:Presentation.Views"
        xmlns:cm="http://caliburnmicro.com"
        xmlns:viewModels="clr-namespace:Presentation.ViewModels"
        mc:Ignorable="d"
        Title="LogIn" Height="250" Width="400"
        cm:Bind.AtDesignTime="True"
        d:DataContext="{d:DesignInstance viewModels:LoginViewModel}">

    <metro:MetroWindow.Resources>

        <Style x:Key="{x:Type PasswordBox}" TargetType="{x:Type PasswordBox}">
            <Setter Property="local:PasswordBoxMonitor.IsMonitoring" Value="True"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type PasswordBox}">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" 
                                        BorderThickness="0" 
                                        Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost" Focusable="False" 
                                              HorizontalScrollBarVisibility="Hidden" 
                                              VerticalScrollBarVisibility="Hidden"/>
                                <Viewbox HorizontalAlignment="Left">
                                    <TextBlock x:Name="InternalWatermarkLabel" Margin="1,3" Width="179"
                                                   Text="{TemplateBinding Tag}" 
                                                   Visibility="Collapsed" Focusable="False"
                                                   Foreground="Black"/>
                                </Viewbox>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsFocused" Value="False" />
                                    <Condition Property="local:PasswordBoxMonitor.PasswordLength" Value="0" />
                                </MultiTrigger.Conditions>
                                <MultiTrigger.Setters>
                                    <Setter Property="Visibility" TargetName="InternalWatermarkLabel" Value="Visible"/>
                                </MultiTrigger.Setters>
                            </MultiTrigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" TargetName="border" Value="0.56"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" TargetName="border" Value="#FF7EB4EA"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="BorderBrush" TargetName="border" Value="#FF569DE5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnInfoStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="#48D1CC"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                SnapsToDevicePixels="True" CornerRadius="5">
                            <Viewbox>
                                <TextBlock Margin="4" Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}" 
                                       VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Viewbox>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="	#40E0D0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="	#20B2AA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnTextStyle" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="#002c99"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Viewbox>
                            <TextBlock x:Name="txt" Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Viewbox>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#FF2c99" TargetName="txt"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Foreground" Value="#002c99" TargetName="txt"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </metro:MetroWindow.Resources>

    <Grid>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="85*"></ColumnDefinition>
                <ColumnDefinition Width="21*"></ColumnDefinition>
                <ColumnDefinition Width="181*"/>
                <ColumnDefinition Width="21*"/>
                <ColumnDefinition Width="85*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="49*"/>
                <RowDefinition Height="36*"/>
                <RowDefinition Height="36*"/>
                <RowDefinition Height="23*"/>
                <RowDefinition Height="36*"/>
                <RowDefinition Height="31*"></RowDefinition>
            </Grid.RowDefinitions>

            <Image Grid.ColumnSpan="5" Grid.RowSpan="6" Source="Images\background3.jpeg" Stretch="Fill" 
                   VerticalAlignment="Center" Margin="0"/>

            <Border BorderBrush="	#F0F8FF" BorderThickness="2" CornerRadius="10" Grid.Row="1" Grid.Column="1"
                    Margin="0,0,0,0" Grid.RowSpan="4" Grid.ColumnSpan="3"/>

            <Border BorderBrush="#FFFFFF" Grid.Row="1" Grid.Column="2" Margin="0,10,0,0"
                    BorderThickness="2" CornerRadius="5" Background="#F0F8FF" Opacity="0.5" >
                <Viewbox HorizontalAlignment="Left"  >
                    <TextBox Name="User" BorderThickness="0" Width="179">
                        <TextBox.Resources>
                            <VisualBrush x:Key="HintText" TileMode="None" Opacity="1"  Stretch="None" 
                                         AlignmentX="Left">
                                <VisualBrush.Visual>
                                    <Viewbox>
                                        <TextBlock Text="用户名"  Foreground="Black" />
                                    </Viewbox>
                                </VisualBrush.Visual>
                            </VisualBrush>
                        </TextBox.Resources>
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="{x:Null}">
                                        <Setter Property="Background" Value="{StaticResource HintText}" />
                                    </Trigger>
                                    <Trigger Property="Text" Value="">
                                        <Setter Property="Background" Value="{StaticResource HintText}" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </Viewbox>

            </Border>

            <Border BorderBrush="#FFFFFF" Grid.Row="2" Grid.Column="2"  Margin="0,10,0,0" 
                    BorderThickness="2" CornerRadius="5" Background="#F0F8FF" Opacity="0.5">
                <PasswordBox Name="Password" Background="Transparent" Tag="密码"/>
            </Border>

            <Button x:Name="TurnToRegister"  Grid.Row="3" Grid.Column="2" Background="Transparent"
                       Margin="155.667,4.667,0,4"  BorderBrush="Transparent" Content="注册"
                       Foreground="White" Style="{StaticResource BtnTextStyle}">
            </Button>

            <Button x:Name="LoginBtn" Content="登录" Grid.Column="2" Grid.Row="4"
                    Margin="0,3,0,10" Style="{StaticResource BtnInfoStyle}"/>


        </Grid>
    </Grid>
</metro:MetroWindow>
